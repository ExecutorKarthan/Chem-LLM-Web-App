# =============================================================================
# backend/backend_project/.env.template
#
# Copy this file to .env and fill in your values before running the app.
# The .env file is listed in .gitignore and should NEVER be committed to git.
#
# Usage:
#   cp backend/backend_project/.env.template backend/backend_project/.env
#   # Then edit .env with your real values
# =============================================================================

# ── Django Secret Key ─────────────────────────────────────────────────────────
# Generate one with:
#   python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Or the deploy.sh script generates this automatically on first run.
DJANGO_SECRET_KEY=your-secret-key-here

# ── Debug Mode ────────────────────────────────────────────────────────────────
# Set to True for local development, False for production.
# WARNING: Never run with DEBUG=True in production.
DJANGO_DEBUG=True

# ── Production Domain ─────────────────────────────────────────────────────────
# The domain your app is served from in production.
# Used in ALLOWED_HOSTS and CORS settings.
# Examples:
#   your-app.your-university.edu
#   myapp.example.com
#   192.168.1.100   (IP address if no domain yet)
#
# !! REPLACE this with your real domain before deploying !!
PRODUCTION_DOMAIN=your-app.your-domain.com

# File cache directory (created automatically at startup)
# Change this if /tmp is too small on your system
DJANGO_CACHE_DIR=/tmp/django_cache
